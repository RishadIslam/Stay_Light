<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rishad.stay_light">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupActivity" />
        <activity android:name=".ForgetActivity" />
        <activity android:name=".CustomToast" />
        <activity android:name=".homelist_1" />
        <activity android:name=".homelist_2" />
        <activity android:name=".homelist_3" />
        <activity android:name=".homelist_4" />
        <activity android:name=".rentlistsuccess" />
        <activity android:name=".booking" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDE9i61ysbRc59oqISlIfr7mu5roLIe2Wg" />

        <activity android:name=".MapsActivity" />
        <activity
            android:name=".HomePage_Map"
            android:label="@string/title_activity_home_page__map" />
        <activity android:name=".UploadImageRoom" />
        <activity android:name=".ImagesActivity" />
        <activity android:name=".SearchActivity"></activity>
    </application>

</manifest>